#! /usr/bin/ruby

require "cheetah"
require "yaml"

require "ucmt/discovery/local_users"

def write(io, data)
  io.puts(data.to_yaml)
end

users = UCMT::Discovery::LocalUsers.new.read_data
write(STDOUT, users)
